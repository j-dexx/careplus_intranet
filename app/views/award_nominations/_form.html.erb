<%= form_for(@award_nomination) do |f| %>
  <% if @award_nomination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@award_nomination.errors.count, "error") %> prohibited this award_nomination from being saved:</h2>

      <ul>
      <% @award_nomination.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :award %><br />
    <%= f.text_field :award %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
